# POC Readme
## Project Setup
```
➜  react-component-library git:(vite-component-library-setup) ✗ npm create rslib@latest
Need to install the following packages:
create-rslib@0.19.2
Ok to proceed? (y) y


> react-component-library-poc@0.0.0 npx
> create-rslib


◆  Create Rslib Project
│
◇  Project name or path
│  rslib-component-library
│
◇  Select template
│  React
│
◇  Select language
│  TypeScript
│
◇  Select development tools (Use <space> to select, <enter> to continue)
│  Storybook, Rstest
│
◇  Select additional tools (Use <space> to select, <enter> to continue)
│  ESLint - linting, Prettier - formatting
│
◇  Next steps ────────────────────╮
│                                 │
│  1. cd rslib-component-library  │
│  2. git init (optional)         │
│  3. npm install                 │
│  4. npm run dev                 │
│                                 │
├─────────────────────────────────╯
│
└  All set, happy coding!
```

# Generated Readme
# Rslib project

## Setup

Install the dependencies:

```bash
npm install
```

## Get started

Build the library:

```bash
npm run build
```

Build the library in watch mode:

```bash
npm run dev
```
